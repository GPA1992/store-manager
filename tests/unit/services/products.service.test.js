const { expect } = require('chai');
const { productServices } = require('../../../src/services')
const sinon = require('sinon');
const connection = require('../../../src/models/connection');



describe('Teste de unidade da camada model', function () {
  describe('Testando o service de produtos', function () {
    it('Testa a função getProducts, que deve retornar a lista de todos os produtos', function () {
      // arrange
      // act
      // assert
    });
    it('Testa a função getProductById, que deve retornar um produto pelo ID', function () {
      // arrange
      // act
      // assert
    });
    it('Testa a função insertNewProduct, que deve inserir um novo produto', function () {
      // arrange
      // act
      // assert
    });
    it('Testa a função insertNewProduct, que deve inserir um novo produto', function () {
      // arrange
      // act
      // assert
    });
  });
});
